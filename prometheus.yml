global:
  scrape_interval: 15s # Tiempo entre cada recolección de métricas

scrape_configs:
  - job_name: 'catalog-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8081'] # host.docker.internal permite acceder al microservicio si corre fuera de docker
